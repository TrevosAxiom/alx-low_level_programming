#include "main.h"

/**
* _isupper - check for upper case letter
* @c : character to check the case
* Return:0 or 1
*/

int _isupper(int c)
{
int num = 0;
if (c >= 65)
{
num = 1;
}
else if (c <= 90)
{
num = 0;
}
return (num);
}
