#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * main - Main Entrance
 * Return: 0 because it's a void
 */
int main(void)
{
int num;
while (num <= 10)
{
printf("%d, ", num);
num++;
}
return (0);

}
